const grid=document.getElementById('menuGrid');const buttons=document.querySelectorAll('.filters button');function render(filter='All'){grid.innerHTML='';MENU_ITEMS.filter(i=>filter==='All'||i.category===filter).forEach(item=>{const card=document.createElement('article');card.className='card';card.innerHTML=`<img src="${item.image}" alt="${item.name}"><div class="card-body"><div class="card-top"><h3>${item.name}</h3><span class="price">AED ${item.price}</span></div><p>${item.description}</p><span class="tag">${item.category}</span></div>`;grid.appendChild(card);});}buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');render(btn.dataset.filter);}));render();